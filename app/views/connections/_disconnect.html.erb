<%= form_for(current_user.active_relationships.find_by(connected_user_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Disconnect", class: "btn" %>
<% end %>